# SMTP Email Settings
# Чтобы включить реальную отправку email на почту пользователей:
# 1. Скопируйте этот файл: cp .env.example .env
# 2. Заполните настройки SMTP ниже
# 3. Перезапустите: docker-compose down && docker-compose up -d

# Yandex Mail (рекомендуется)
SMTP_USERNAME=your-email@yandex.ru
SMTP_PASSWORD=your-app-password-from-yandex
SMTP_FROM_EMAIL=your-email@yandex.ru
SMTP_ENABLED=True

# Или Gmail
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password-from-gmail
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_ENABLED=True
